load("//bazel/macros:hugo.bzl", "hugo_site")
load("@bazel_tools//tools/build_defs/pkg:pkg.bzl", "pkg_tar")

pkg_tar(
    name = "build",
    srcs = glob(["*", "**/*"]),
    strip_prefix = "./"
)

hugo_site(
    name = "website",
    config = ":config.yml",
    src = ":build",
)